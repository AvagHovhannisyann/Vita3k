// JitDiagnosticsViewController.h — runs every known route to executable memory
// and reports exactly what this iOS version allows.
#import <UIKit/UIKit.h>
@interface JitDiagnosticsViewController : UIViewController
@end
